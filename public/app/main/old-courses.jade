div(ng-repeat="course in courses | orderBy: '-published' | limitTo: 10")
    .row
        .col-md-3
            | {{ course.published | date: 'MMM d'}}
        .col-md-9
            | {{ course.title}}